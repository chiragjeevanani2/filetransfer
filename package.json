{
  "name": "localdrop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "serve": "npm run build --prefix frontend && npm run serve --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
